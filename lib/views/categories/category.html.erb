<div class="jumbotron">
  <div class="container">
    <h1 class="display-title text-center">Courses in <%= category.title %></h1>
  </div>
</div>

<div class="container">
  <div class="cards">
    <% courses.each do |course| %>
      <%= render_partial '_card', object: course, link_text: 'Start this course', external_link: false %>
    <% end %>
  </div>
</div>
